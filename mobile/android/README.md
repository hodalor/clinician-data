Android is the initial target for this Flutter client.

The native Android runner was not generated here because the Flutter SDK is not installed in this environment.

Generate it later with:

```bash
flutter create --platforms=android .
```
